rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For this single-company application, any authenticated user can read/write all data.
      // Access is controlled by who has an account, which is managed by the admin.
      allow read, write: if request.auth != null;
    }
  }
}